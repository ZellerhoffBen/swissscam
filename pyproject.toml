[project]
name = "swissscam"
version = "0.1.0"
description = "Local scam-call detection skeleton"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "fastapi",
    "faster-whisper",
    "pydantic",
    "python-multipart",
    "python-dotenv",
    "scikit-learn>=1.7,<2",
    "uvicorn",
    "whisperx>=3.8.6",
]
